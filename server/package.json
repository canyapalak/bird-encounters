{
  "name": "server",
  "version": "1.0.0",
  "description": "bird-encounters app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon server.js",
    "bothServers": "concurrently \"nodemon server.js\" \"cd ../client && npm start\""
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
